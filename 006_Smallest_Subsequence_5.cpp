#include <iostream>

using namespace std;
int N, K;
string S;
int main(){
    cin >> N >> K;
    cin >> S;
    string res = "z";
    for (int i = 0; i < N; i++) {
        for (int j = K - max(K, N - i); j < K; j++) {

        }
    }
    return 0;
}